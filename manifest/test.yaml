apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: azure-vote-back3
spec:
  replicas: 3
  selector:
    matchLabels:
      app: azure-vote-back3
  template:
    metadata:
      labels:
        app: azure-vote-back3
    spec:
      nodeSelector:
        "beta.kubernetes.io/os": linux
      containers:
      - name: azure-vote-back
        image: mcr.microsoft.com/oss/bitnami/redis:6.0.8
        env:
        - name: ALLOW_EMPTY_PASSWORD
          value: "yes"
        ports:
        - containerPort: 6379
          name: redis
---
apiVersion: v1
kind: Service
metadata:
  name: azure-vote-back3
spec:
  ports:
  - port: 6379
  selector:
    app: azure-vote-back3
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: azure-vote-front3
spec:
  replicas: 3
  selector:
    matchLabels:
      app: azure-vote-front
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5 
  template:
    metadata:
      labels:
        app: azure-vote-front
    spec:
      nodeSelector:
        "beta.kubernetes.io/os": linux
      containers:
      - name: azure-vote-front3
        image: mcr.microsoft.com/dotnet/samples:aspnetapp-alpine-slim-amd64
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: 250m
          limits:
            cpu: 500m
        env:
        - name: REDIS
          value: "azure-vote-back3"
---
apiVersion: v1
kind: Service
metadata:
  name: azure-vote-front
spec:
  type: LoadBalancer
  ports:
  - port: 80
  selector:
    app: azure-vote-front
